name: "AutoSpec Evaluator"
description: "Evaluate AutoSpec-generated ERDs against the PRD (Step 1), optional engineering ERD comparison (Step 2: scope gate then full-scope or reduced-scope/same-delivery analysis), optional System Context comparison between two ERDs (Step 4), and optional Excel export bundling all completed steps"

slug: autospec-evaluator

model: "GPT-5.4 Thinking"

capabilities:
  - canvas
  - code_interpreter_and_data_analysis

conversation_starters:
  - "I have a PRD and a generated ERD to evaluate"
