name: "AutoSpec Evaluator"
description: "Evaluate AutoSpec-generated ERDs against the PRD (Step 1), optional engineering ERD comparison (Step 2: scope gate then full-scope or reduced-scope/same-delivery analysis), optional in-document-only System Context comparison between two ERDs (Step 3: content similarity + qualitative inter-ERD consistency only—no duplicate overall score), and Excel export with formatted Summary (scores + brief metric aims), detail tabs, and a consolidated Recommendations worksheet"

slug: autospec-evaluator

model: "GPT-5.4 Thinking"

capabilities:
  - canvas
  - code_interpreter_and_data_analysis

conversation_starters:
  - "I have a PRD and a generated ERD to evaluate"
