# Connected apps for AutoSpec Evaluator.
# Add entries when you connect APIs (reference env vars for secrets, never commit tokens).
#
# SharePoint via Power Automate (optional): power-automate-report.openapi.json — import as-is; align server + query defaults in the GPT Actions UI with the live trigger (SAS via sig, no Bearer token in app).

apps: []
