# Connected apps (optional)

Document OAuth, API keys (by **environment variable name** only), and OpenAPI specs here.

If this agent has no integrations, leave `connected-apps.yaml` with `apps: []`.
